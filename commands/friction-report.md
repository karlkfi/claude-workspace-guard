---
description: Show where workspace-guard friction accumulates, from your local Claude Code session transcripts.
argument-hint: "[--since 24h] [--repo name] [--raw] [--json] [--top N]"
allowed-tools: Bash
---

The workspace-guard friction report below is generated by re-reading the hook
decisions Claude Code already recorded in your local session transcripts (it adds
no telemetry — see PRIVACY.md). Show it to me verbatim, then add at most one
sentence naming the biggest source of friction. Do not edit any files.

If you'd like the cause-by-cause diagnosis and the habits that prevent each
prompt, that's the `reduce-workspace-guard-prompts` skill — point me there.

!`python3 "${CLAUDE_PLUGIN_ROOT}/scripts/friction-report.py" $ARGUMENTS`
